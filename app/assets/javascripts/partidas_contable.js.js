// Generated by CoffeeScript 1.6.2
(function() {
  jQuery(function() {
    var bancos, llenarBanco;

    llenarBanco = function(bancos) {
      var empresa, options;

      empresa = $('#partida_contable_empresa_id :selected').text();
      options = $(bancos).filter("optgroup[label='" + empresa + "']").html();
      if (options) {
        $('#partida_contable_banco_id').html('<option value="">Seleccione un banco...</option>');
        return $('#partida_contable_banco_id').append(options);
      } else {
        return $('#partida_contable_banco_id').empty();
      }
    };
    bancos = $('#partida_contable_banco_id').html();
    llenarBanco(bancos);
    return $('#partida_contable_empresa_id').change(function() {
      return llenarBanco(bancos);
    });
  });

}).call(this);
