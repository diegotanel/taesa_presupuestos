<li>
  <%= gravatar_for user, size: 30 %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to "eliminar", user, :id => "delete_#{user.id}" ,:method => :delete, confirm: "esta seguro?", :title => "Delete #{user.name}" %>
  <% end %>
</li>
