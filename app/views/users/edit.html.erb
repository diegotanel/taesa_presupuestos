<h1><%= @title %></h1>

<%= form_for(@user) do |f| %>
  <%= render 'fields', :f => f, :labelSubmit => "Actualizar" %>
<% end %>

<div>
  <%= gravatar_for @user %>
  <a href="http://gravatar.com/emails">cambiar</a>
</div>
